import React from 'react'
import { TouchableOpacity } from 'react-native'

const SelfTouchableOpacity = props => <TouchableOpacity activeOpacity={0.7} {...props} />

export default SelfTouchableOpacity
